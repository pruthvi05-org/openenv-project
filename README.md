# openenv-project
This project implements an OpenEnv-compatible reinforcement learning environment with fully defined step(), reset(), and state() APIs. It includes multiple graded tasks (Easy, Medium, Hard), a Dockerized setup for reproducibility, and an inference pipeline for automated evaluation using LLM-based agents. 
